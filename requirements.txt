# LoRA Easy Training - Jupyter Widget Edition
# Core dependencies for the widget-based training system

# Jupyter ecosystem
jupyter>=1.0.0
ipywidgets>=8.0.0
notebook>=6.4.12
ipython>=8.0.0

# Core Python packages
toml>=0.10.2
requests>=2.28.0
tqdm>=4.64.0
Pillow>=9.0.0

# Data handling
numpy>=1.21.0
pandas>=1.5.0

# Optional: For enhanced dataset management
# huggingface-hub>=0.10.0  # Uncomment if using HF datasets directly

# Note: Training dependencies (PyTorch, etc.) are installed
# by the training backend during environment setup