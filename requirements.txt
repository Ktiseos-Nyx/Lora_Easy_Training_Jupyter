# LoRA Easy Training - Jupyter Widget Edition
# Core dependencies required for the notebook UI to function.
# All backend training dependencies are in requirements-backend.txt

ipywidgets>=8.0.0
ipython>=8.0.0
sidecar>=0.5.0
toml>=0.10.2
fiftyone
